---
import AccordionItem from "./AccordionItem.astro";

import content from "../../data/process.json";
---

{
  content.map((item, i) => {
    return (
      <AccordionItem
        index={i + 1}
        title={item.title}
        description={item.description}
      />
    );
  })
}
